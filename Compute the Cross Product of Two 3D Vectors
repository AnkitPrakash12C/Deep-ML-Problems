import numpy as np

def cross_product(a, b):
    l = len(a)
    res = []
    for i in range(l-1):
        temp = (a[i] * b[i+1]) - (a[i+1]*b[i])
        res.append(temp)
    res = res[::-1]
    ind = len(res)//2
    res.insert(ind,(a[l-1]*b[0])-(a[0]*b[l-1]))
    return res
    pass
